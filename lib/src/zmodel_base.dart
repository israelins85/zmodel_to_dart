/// Base contract implemented by generated model classes.
abstract class ZModel {
  /// Creates the base model contract.
  const ZModel();

  /// Converts the model instance to a JSON-compatible map.
  Map<String, dynamic> toJson();
}
